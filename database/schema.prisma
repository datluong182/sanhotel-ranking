datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model tbObjectTrips {
  id           Int    @id @default(autoincrement())
  name         String
  url          String
  rank         String[]
  scoreReview  Int
  totalReviews Int
}
